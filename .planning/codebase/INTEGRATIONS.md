# Integrations & External Services

## Active External Integrations
- **None currently configured:** The repository is in its initial bootstrap state from `create-next-app`.

## Future / Candidate Integrations for RepoLens
- **GitHub / Git Provider API:** For repository inspection, PR analysis, commit inspection, code scanning, or repository metadata fetching.
- **AI / LLM Providers:** Gemini / OpenAI / Anthropic APIs for repository intelligence and code summarization (if applicable).
- **Authentication / Database:** OAuth / Database integrations (e.g. Supabase, PostgreSQL, SQLite, or Prisma/Drizzle) as features are scoped.

## Environmental Configuration
- No `.env` or `.env.local` files currently present.
